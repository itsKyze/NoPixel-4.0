{
  "name": "np-commands",
  "version": "1.0.0",
  "description": "NoPixel 4.0 - np-commands",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "typecheck": "tsc --noEmit"
  }
}